import React from "react";

export function Toaster() {
  // This is now just a placeholder since we're using Sonner
  // Using div with display:none instead of Fragment to prevent data-lov-id warning
  return <div style={{ display: 'none' }}></div>;
}
